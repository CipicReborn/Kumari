package com.isartdigital.utils.events;
import pixi.utils.EventTarget;

/**
 * Classe "hack" permettant d'étendre EventTarget qui n'est pas une classe en Javascript
 * Permet de compenser son comportement un peu particulier
 * @author Mathieu ANTHOINE
 */
class EventTarget extends pixi.utils.EventTarget
{
	
}